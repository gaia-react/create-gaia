# create-gaia

Scaffold a new project from [GAIA React](https://gaiareact.com/) — the Claude Code workflow for React. Source on [GitHub](https://github.com/gaia-react/gaia).

## Usage

```bash
npx create-gaia my-app
```

Then open the new project in Claude Code and run `/init`.

The scaffold is intentionally minimal — post-setup happens via the `/init` command inside Claude Code.

## License

MIT
